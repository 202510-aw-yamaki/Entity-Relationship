# 未達要件

## この文書の役割
- この文書には、現時点で未達の要件だけを記述します。
- この文書にある項目を達成したら、この文書から削除し、`docs/COMPLETED_REQUIREMENTS.md` へ追記してください。
- 達成済み扱いにしてよいか迷う項目があるときは、この文書と `index.html` を確認してください。

---

## 未達の項目

### 選択状態の明確化
- 箱内要件を選択したときに、箱や線と同じ粒度で「選択中」と分かる表示
- 現在状態の要約や詳細編集で、箱内要件を独立した選択対象として扱える状態

---

## 追記
- 2026-04-13
  - 文書の役割を明確化し、達成した項目は達成済み文書へ移す運用に更新した。
- 2026-04-13
  - 今回追加された `Ctrl / Cmd + クリック` 接続、左右パネルの開閉、ツールバー内 STEP ガイドとショートカット表示は実装済みのため、この文書には残していない。

## 2026-04-14 追記: UI / 操作性メモ

- 箱を選択した状態で `Ctrl+C` でコピーし、`Ctrl+V` で複製できるようにしたい
- 箱を置いて作業する図エリアが狭いため、作業領域を拡張したい
- 各 STEP 判定はサイドバーを開かなくても実行できるようにしたい
- 判定ショートカット案として `Ctrl+S1` で STEP1、`Ctrl+S2` で STEP2、`Ctrl+S3` で STEP3 を実行できるようにしたい
- 箱の要件へ追加入力しようとすると 1 文字でフォーカスが外れるため、連続入力できるように修正したい

## 2026-04-14 追記: ユーザー要望

- 現状の、AIへ貼る文章をコピペしてAIに張るとAIのチャット上にJSON記述でそのまま出力されるので、『記述したJSONファイルをダウンロードリンクで出してください』という文面を含めてほしい