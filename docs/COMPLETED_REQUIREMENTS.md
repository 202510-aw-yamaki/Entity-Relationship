# COMPLETED_REQUIREMENTS

## この文書の扱い
- この文書は、2026-04-13 時点の `index.html` を根拠に、実装済みと確認できた要件だけを整理したものです。
- 運用効果や使いやすさの保証は含めず、コードから確認できる範囲に限定します。
- 2026-04-13 の要望で `AGENTS.md` から達成済み要件を分離しました。

---

## 達成済み要件

### 基本構成
- ローカルでブラウザから開ける単一 HTML 構成
- 左 / 中央 / 右の 3 カラム構成
- 日本語 UI
- 開始時に空の状態 / JSON読込 / サンプル開始を選べる開始画面

### 箱・親箱・箱内要件
- 図エリアのドラッグで通常箱を作れる
- ボタンから通常箱を追加できる
- 親箱作成モードで親箱 / グループ箱を作れる
- 箱をドラッグ移動できる
- 箱をリサイズできる
- 箱のカテゴリ、タイトル、意味合い、顧客向け注釈を編集できる
- 通常箱を親箱に所属させられる
- 箱を親箱内へ動かしたときに所属を自動補正できる
- 箱内要件の追加 / 編集 / 削除ができる

### 線
- 箱同士を線でつなげられる
- 箱内要件同士を線でつなげられる
- 線の関係種別、説明ラベル、`arrowStart`、`arrowEnd` を編集できる
- 選択中の箱 / 線は画面上で判別できる

### 保存・読込・出力
- 保存用 JSON をダウンロードできる
- 保存用 JSON を読込んで状態復元できる
- 旧形式の `boxes` / `lines` / `connections` を吸収して読込める
- 不正な親箱参照や存在しない接続先を安全側に補正して読込める
- AI向け Markdown を `_AI.md` で出力できる
- human向け Markdown を `_human.md` で出力できる
- AI相談用 JSON を `_AI相談.json` で出力できる

### STEP と補助機能
- STEP1 / STEP2 / STEP3 を切り替えて確認できる
- 各 STEP の判定を日本語文章で表示できる
- 警告が残った状態で STEP2 / STEP3 へ進むと確認ポップアップを出せる
- STEP3 を完了扱いにする前に確認ポップアップを出せる
- 現在状態の要約を右カラムに表示できる
- Undo / Redo が使える
- ズームが使える

---

## 追記
- 2026-04-13
  - ユーザー要望により、`AGENTS.md` から達成済み要件を分離した。
